# Catalog assets（materialize 先）

`setup-runtime` / `graphassist assets fetch`（Phase S2）実行後に、ここへ SVG と rasterized PNG が配置されます。

- 正本定義: `.rulesync/metadata/asset-catalog.jsonc`
- LLM 索引: `samples/jobs/catalog/index.json`

バイナリ（`.svg` / `.png`）は Git 管理外です。README のみ tracked。
