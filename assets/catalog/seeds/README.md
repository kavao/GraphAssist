# Catalog seeds（Git 正本）

リポジトリ同梱のカタログ素材。`asset-catalog.jsonc` の `release.local` から mirror されます。

| ファイル | id | 用途 |
|----------|-----|------|
| `ui-panel-dark.svg` | `ui-panel-dark` | カード / バッジ土台（400×600、角丸） |

外部 URL 素材と同様、fetch 後は `samples/source/catalog/` に PNG mirror されます。
